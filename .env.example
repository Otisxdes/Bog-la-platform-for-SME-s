# Database Configuration
DATABASE_URL="file:./dev.db"

# Next.js Configuration (optional for development)
# NEXTAUTH_SECRET="your-secret-key-here"
# NEXTAUTH_URL="http://localhost:3000"

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. The DATABASE_URL is required for Prisma to work
# 3. Run: npm run db:migrate (creates the database)
# 4. Run: npm run db:seed (creates test seller account)
