# Database Configuration
# For local development with PostgreSQL:
DATABASE_URL="postgresql://user:password@localhost:5432/bogla_dev?schema=public"

# Or use a cloud PostgreSQL provider:
# Vercel Postgres: postgres://user:password@region.postgres.vercel-storage.com:5432/database
# Supabase: postgresql://postgres:password@db.project.supabase.co:5432/postgres
# Neon: postgresql://user:password@region.neon.tech/database

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DATABASE_URL with your PostgreSQL connection string
# 3. Run: npm install (generates Prisma client)
# 4. Run: npm run db:migrate (creates the database schema)
# 5. Run: npm run db:seed (creates test seller account)
#
# For Vercel deployment:
# - Set DATABASE_URL as an environment variable in Vercel dashboard
# - Make sure your PostgreSQL database is accessible from Vercel

# Cloudinary Configuration (for image uploads)
# Sign up at https://cloudinary.com for free account
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
