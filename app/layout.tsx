import './globals.css';

export const metadata = {
  title: "Bog'la for SME's",
  description: 'Payment links and order management for small businesses',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
